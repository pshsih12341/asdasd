import React from 'react';
import type { AlertContextProviderProps } from './types';
export declare const AlertContextProvider: ({ layout, view, children }: AlertContextProviderProps) => React.JSX.Element;
