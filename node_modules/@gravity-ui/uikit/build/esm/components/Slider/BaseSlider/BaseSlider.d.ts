import React from 'react';
import type { SliderProps, SliderRef } from 'rc-slider';
import type { StateModifiers } from '../types';
import './BaseSlider.css';
export declare const BaseSlider: React.ForwardRefExoticComponent<{
    stateModifiers: StateModifiers;
} & Omit<SliderProps<number | number[]>, "className" | "classNames" | "prefixCls" | "keyboard" | "pushable"> & React.RefAttributes<SliderRef>>;
