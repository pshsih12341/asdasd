import type { SliderInnerState, SliderProps } from './types';
/**
 * Calculates the basic properties of the Slider component depending on the passed parameters
 * @returns {SliderInnerState} Properties to pass to the Slider
 */
export declare function prepareSliderInnerState({ max, min, availableValues, defaultValue, marksCount, step, value, markFormat, marks, hasTooltip, tooltipDisplay, tooltipFormat, }: {
    max: number;
    min: number;
} & Pick<SliderProps, 'availableValues' | 'defaultValue' | 'marksCount' | 'step' | 'value' | 'markFormat' | 'marks' | 'hasTooltip' | 'tooltipDisplay' | 'tooltipFormat'>): SliderInnerState;
