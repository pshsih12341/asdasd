import React from 'react';
import type { LayoutTheme } from '../../../layout';
interface LayoutPresenterProps {
    children?: React.ReactNode;
    title?: string;
    theme?: LayoutTheme;
}
export declare const LayoutPresenter: ({ children, title, theme: config }: LayoutPresenterProps) => React.JSX.Element;
export {};
