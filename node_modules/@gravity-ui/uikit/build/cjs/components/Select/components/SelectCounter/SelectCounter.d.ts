import React from 'react';
import type { SelectCounterProps } from '../../types';
import './SelectCounter.css';
export declare function SelectCounter({ count, size, disabled }: SelectCounterProps): React.JSX.Element;
