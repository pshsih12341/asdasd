import React from 'react';
import type { AvatarImageProps } from './types';
export declare const AvatarImage: ({ imgUrl, fallbackImgUrl, sizes, srcSet, alt, loading, size, className, }: AvatarImageProps) => React.JSX.Element;
